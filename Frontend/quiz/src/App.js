import React, {Component} from 'react';

import Routes from './routes';

//Componente que a tela inicial, definida em routes
export default class App extends Component{
  render(){
    return(
      <Routes />
    );
  }
}